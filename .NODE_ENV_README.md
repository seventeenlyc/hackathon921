# Node 16 虚拟环境（.nodeenv）

项目原本基于 2020 年的打包器 parcel-bundler v1，它依赖原生模块 **deasync**。
在系统当前 Node v24 下：

- deasync 找不到对应预编译二进制，会尝试用 node-gyp 现场编译；
- 本机没有 VS C++ Build Tools，编译失败，导致 `npm install` 整体报错 `spawn EINVAL`。


## 一键启动（推荐）

双击项目根目录的 **`start.bat`** 即可：
- 自动检查/安装依赖；
- 启动 parcel dev server 到 http://localhost:1234；
- 约 8 秒后自动用默认浏览器打开 **http://localhost:1234/index.html**（parcel v1 的 Pug 入口根路径会 404，故固定访问 /index.html）；
- 关闭 dev server 所在窗口（或按 Ctrl+C）即可停止。


## 解决方案

为项目创建了一个**项目本地虚拟 Node 环境**，放在 `.nodeenv\node-v16.20.2-win-x64`（Node 16 LTS）。
配合 package.json 里的 `overrides` 把 deasync 固定到 **0.1.31**——该版本内置了
`win32-x64-node-16/deasync.node` 预编译二进制，完全不需要 node-gyp 编译。

## 使用方法

在项目根目录（D:\\inert）执行：

```cmd
call .nodeenv\node-v16.20.2-win-x64\npm.cmd install
call .nodeenv\node-v16.20.2-win-x64\npm.cmd run build
```

或先激活环境再直接用 node/npm：

```cmd
call .\.nodeenv\activate.cmd
npm install
npm run build
```

> 注意：parcel-bundler v1 会 fork 子进程，在受限沙箱下可能报 `spawn EPERM`；
> 需要放开进程/文件权限后再构建。
>
> 若以后想用系统默认 Node，直接不用 activate 即可；构建必须使用 16（或匹配 deasync 预编译的 ABI）。

## 开发服务器（dev）访问地址

本项目入口是 Pug（`.pug`）。parcel-bundler v1 的 dev server 对非 HTML 入口有一个已知限制：
**`http://localhost:1234/` 会返回 404**，页面实际挂在 `/index.html`。

开发服务器打开的正确地址是：

```
http://localhost:1234/index.html
```

（页面上引用的 JS / CSS / 图片 / manifest 等资源都已验证全部正常返回 200。）
